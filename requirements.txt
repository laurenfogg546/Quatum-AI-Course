# Build-time dependencies for the JupyterLite site.
# These are installed by the GitHub Actions runner to BUILD the site.
# Students never install anything — everything runs in their browser.
jupyterlite-core>=0.4.0,<0.6.0
jupyterlite-pyodide-kernel>=0.4.0,<0.6.0
jupyterlab~=4.2
notebook~=7.2
